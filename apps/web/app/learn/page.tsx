import { LearningHub } from "@/components/lessons/hub";

export const metadata = {
  title: "Learning Hub",
  description: "Short, interactive finance lessons that build on the simulators.",
};

export default function LearnPage() {
  return <LearningHub />;
}
