# Training-side dependencies (docs/08: ml/ is offline-only).
# The API serving deps live in apps/api/requirements.txt; both install into
# the same repo-root .venv for local development.
pandas==2.2.3
scikit-learn==1.5.2
xgboost==2.1.3
joblib==1.4.2
